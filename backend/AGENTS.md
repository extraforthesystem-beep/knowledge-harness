# Backend Agent Rules (AGENTS.md — Scoped)

> **Lazy-Loaded:** These rules are ONLY injected when the agent modifies files within this `backend/` directory.

## API Conventions
- [Define your API patterns, error handling, auth middleware here]
- [e.g., "All endpoints return `{ data, error, meta }` envelope."]

## Database
- [Migration patterns, ORM conventions, connection pooling rules]

## Security
- [Input validation, rate limiting, secrets management]

## Testing
- Unit tests for all service functions.
- Integration tests for API endpoints.
