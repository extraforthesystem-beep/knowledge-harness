# Frontend Agent Rules (AGENTS.md — Scoped)

> **Lazy-Loaded:** These rules are ONLY injected when the agent modifies files within this `frontend/` directory.

## UI/UX Conventions
- [Define your component library, state management, styling rules here]
- [e.g., "Use React Server Components by default. Client components only for interactivity."]

## Design System
- [Color palette, typography, spacing tokens]
- [e.g., "No purple/violet. See DESIGN.md for approved palette."]

## Testing
- Unit tests with Vitest for all components.
- E2E with Playwright for critical user flows.
